## Scripts

- `prepare_dataset.py` — parses and chunks transcripts into utterances (to be implemented in the next task).
- `create_vector_index.py` — creates MongoDB Atlas Vector Search index on `utterances` (added later).
- `demo.sh` — example run sequence (added later).


